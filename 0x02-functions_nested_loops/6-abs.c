#include "main.h"


/**
 * _abs - Function absolute int
 * @n: absolute value of int
 *
 * Return: Always return 0 as (success)
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
